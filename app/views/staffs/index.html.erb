<h1>スタッフ一覧</h1>

<table>
  <thead>
    <tr>
      <th width="120" >名前</th>
      <th width="240" >説明</th>
      <th width="40"  ></th>
    </tr>
  </thead>

  <tbody>
    <% @staffs.each do |staff| %>
      <tr>
        <td><%= staff.name %></td>
        <td><%= staff.desc %></td>
        <td><%= link_to '詳細', staff %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'メインメニュー', root_path %>

